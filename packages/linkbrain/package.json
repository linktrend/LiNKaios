{
  "name": "@linktrend/linkbrain",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'linkbrain has no build step'",
    "check": "echo 'linkbrain SQL migrations ready'",
    "test": "echo 'linkbrain tests are executed against live Supabase in integration stage'"
  }
}
